// 📁 UI/Dialogs/PluginSettingsDialog.xaml
// ===== شروع کد =====
<UserControl x:Class="TradingJournal.UI.Dialogs.PluginSettingsDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
             Width="500" MaxHeight="600">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <TextBlock Grid.Row="0" 
                   x:Name="TitleText"
                   Text="تنظیمات پلاگین" 
                   Style="{StaticResource MaterialDesignHeadline5TextBlock}"
                   Margin="0,0,0,20"/>
        
        <!-- Settings List -->
        <ScrollViewer Grid.Row="1">
            <ItemsControl x:Name="SettingsContainer">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="0,0,0,16">
                            <ContentControl Content="{Binding}"/>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
        
        <!-- Actions -->
        <StackPanel Grid.Row="2" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right"
                    Margin="0,20,0,0">
            <Button Content="انصراف"
                    Style="{StaticResource MaterialDesignFlatButton}"
                    IsCancel="True"
                    Margin="0,0,10,0"
                    Command="{x:Static md:DialogHost.CloseDialogCommand}">
                <Button.CommandParameter>
                    <system:Boolean>False</system:Boolean>
                </Button.CommandParameter>
            </Button>
            
            <Button Content="ذخیره"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Click="OnSaveSettings"
                    IsDefault="True"/>
        </StackPanel>
    </Grid>
</UserControl>
// ===== پایان کد =====