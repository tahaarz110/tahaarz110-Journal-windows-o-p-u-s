<!-- ابتدای فایل: UI/Themes/CustomStyles.xaml -->
<!-- مسیر: /UI/Themes/CustomStyles.xaml -->

<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">

    <!-- Custom Card Style -->
    <Style x:Key="CustomCard" TargetType="materialDesign:Card">
        <Setter Property="Padding" Value="16"/>
        <Setter Property="Margin" Value="8"/>
        <Setter Property="materialDesign:ShadowAssist.ShadowDepth" Value="Depth2"/>
        <Setter Property="materialDesign:ShadowAssist.ShadowEdges" Value="All"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="materialDesign:ShadowAssist.ShadowDepth" Value="Depth4"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Custom Button Style -->
    <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource MaterialDesignRaisedButton}">
        <Setter Property="Height" Value="36"/>
        <Setter Property="MinWidth" Value="100"/>
        <Setter Property="Margin" Value="4"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="FontWeight" Value="Medium"/>
    </Style>

    <!-- Custom TextBox Style -->
    <Style x:Key="CustomTextBox" TargetType="TextBox" BasedOn="{StaticResource MaterialDesignOutlinedTextBox}">
        <Setter Property="Margin" Value="8,4"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="materialDesign:HintAssist.IsFloating" Value="True"/>
    </Style>

    <!-- Custom ComboBox Style -->
    <Style x:Key="CustomComboBox" TargetType="ComboBox" BasedOn="{StaticResource MaterialDesignOutlinedComboBox}">
        <Setter Property="Margin" Value="8,4"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="materialDesign:HintAssist.IsFloating" Value="True"/>
    </Style>

    <!-- Custom DatePicker Style -->
    <Style x:Key="CustomDatePicker" TargetType="DatePicker" BasedOn="{StaticResource MaterialDesignOutlinedDatePicker}">
        <Setter Property="Margin" Value="8,4"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="materialDesign:HintAssist.IsFloating" Value="True"/>
    </Style>

    <!-- Custom DataGrid Style -->
    <Style x:Key="CustomDataGrid" TargetType="DataGrid" BasedOn="{StaticResource MaterialDesignDataGrid}">
        <Setter Property="AutoGenerateColumns" Value="False"/>
        <Setter Property="CanUserAddRows" Value="False"/>
        <Setter Property="GridLinesVisibility" Value="Horizontal"/>
        <Setter Property="HeadersVisibility" Value="Column"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="RowHeight" Value="40"/>
        <Setter Property="materialDesign:DataGridAssist.CellPadding" Value="16,8"/>
        <Setter Property="materialDesign:DataGridAssist.ColumnHeaderPadding" Value="16,10"/>
    </Style>

    <!-- Tab Header Style -->
    <Style x:Key="TabHeaderStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="FontWeight" Value="Medium"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>

    <!-- Status Bar Item Style -->
    <Style x:Key="StatusBarItemStyle" TargetType="StatusBarItem">
        <Setter Property="Padding" Value="8,0"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>

    <!-- Toolbar Style -->
    <Style x:Key="CustomToolBar" TargetType="ToolBar" BasedOn="{StaticResource MaterialDesignToolBar}">
        <Setter Property="Height" Value="48"/>
        <Setter Property="Padding" Value="8,0"/>
    </Style>

    <!-- Form Group Style -->
    <Style x:Key="FormGroupBorder" TargetType="Border">
        <Setter Property="BorderBrush" Value="{DynamicResource MaterialDesignDivider}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="4"/>
        <Setter Property="Margin" Value="8"/>
        <Setter Property="Padding" Value="12"/>
    </Style>

    <!-- KPI Card Template -->
    <ControlTemplate x:Key="KpiCardTemplate" TargetType="ContentControl">
        <materialDesign:Card Padding="16" Margin="8">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <TextBlock Grid.Row="0" 
                          Text="{TemplateBinding Tag}"
                          FontSize="12"
                          Opacity="0.7"
                          Margin="0,0,0,8"/>
                
                <TextBlock Grid.Row="1"
                          Text="{TemplateBinding Content}"
                          FontSize="28"
                          FontWeight="Bold"/>
                
                <ContentPresenter Grid.Row="2"
                                 Content="{TemplateBinding ContentTemplate}"
                                 Margin="0,8,0,0"/>
            </Grid>
        </materialDesign:Card>
    </ControlTemplate>

    <!-- Loading Spinner Style -->
    <Style x:Key="LoadingSpinner" TargetType="ProgressBar">
        <Setter Property="Style" Value="{StaticResource MaterialDesignCircularProgressBar}"/>
        <Setter Property="Value" Value="0"/>
        <Setter Property="IsIndeterminate" Value="True"/>
        <Setter Property="Width" Value="40"/>
        <Setter Property="Height" Value="40"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>

</ResourceDictionary>

<!-- پایان فایل: UI/Themes/CustomStyles.xaml -->